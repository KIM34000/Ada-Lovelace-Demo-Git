const tableau : string[] = ["flint", "academy", "rebootJs"]

tableau.push("string");

console.log(tableau);

const resultat = ["a", "b", "c"].find((char) => { return char == "a"});
resultat.toString();